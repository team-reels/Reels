//wrapper for bio and dex + other functions
import { useState } from 'react';

function User() {
  const [count, setCount] = useState(0);

  return (
    <>
        User
    </>
  );
};

export default User;
